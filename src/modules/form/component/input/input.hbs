<div class="input-group">
    <p class="ig-title" hidden>{{title}}</p>
    <input class="ig-item" name="{{name}}" type="{{type}}" placeholder="{{title}}">
    <p class="ig-error" hidden>{{error}}</p>
</div>